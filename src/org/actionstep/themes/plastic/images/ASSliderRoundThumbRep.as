/* See LICENSE for copyright and terms of use */

import org.actionstep.constants.NSControlSize;
import org.actionstep.themes.plastic.images.ASHighlightedRadioButtonRep;

/**
 * Draws the round slider.
 * 
 * @author Scott Hyndman
 */
class org.actionstep.themes.plastic.images.ASSliderRoundThumbRep 
		extends ASHighlightedRadioButtonRep {
	/**
	 * Creates a new instance of the <code>ASSliderRoundThumbRep</code> class.
	 */
	public function ASSliderRoundThumbRep(size:NSControlSize) {
		super(size);
	}
	
	private function drawDot():Void {
		
	}
}